{
  "name": "dashy-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test",
    "test:release": "node --test infrastructure/release/verify-version.test.mjs"
  }
}
